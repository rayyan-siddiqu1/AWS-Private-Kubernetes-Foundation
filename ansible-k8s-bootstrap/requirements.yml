---
# =============================================================================
# requirements.yml — Ansible collection dependencies
#
# Install before running any platform plays:
#   ansible-galaxy collection install -r requirements.yml
#
# kubernetes.core provides:
#   kubernetes.core.helm            — Helm chart lifecycle management
#   kubernetes.core.helm_repository — Helm repository management
#   kubernetes.core.k8s             — Kubernetes manifest application
#   kubernetes.core.k8s_info        — Kubernetes resource queries
# =============================================================================

collections:
  - name: kubernetes.core
    version: ">=3.0.0"
