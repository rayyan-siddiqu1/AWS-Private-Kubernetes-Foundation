apiVersion: v1
kind: Namespace
metadata:
  name: test-app
  labels:
    # Consul Connect handles sidecar injection here.
    # Istio injection is disabled to avoid dual-proxy conflicts.
    istio-injection: disabled
