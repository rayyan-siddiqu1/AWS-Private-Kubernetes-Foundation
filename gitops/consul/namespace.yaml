apiVersion: v1
kind: Namespace
metadata:
  name: consul
  labels:
    # Consul Connect handles sidecar injection in this namespace.
    # Istio injection is disabled to avoid dual-proxy conflicts.
    istio-injection: disabled
